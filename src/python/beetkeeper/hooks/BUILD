# httpx is provided by both the app and the test requirements; pin inference to the app's.
python_sources(dependencies=["//src/python:app-requirements#pydantic", "!//3rdparty/python:test-requirements#httpx"])
