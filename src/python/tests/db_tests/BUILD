# `aiosqlite` (and the alembic migration env) now come transitively via the `beetkeeper.db` sources these
# tests import, so no per-target dependency workaround is needed here — the base defaults suffice.
python_tests()
