# `aiosqlite` now comes transitively via the `beetkeeper.db` sources (see `src/python/beetkeeper/db/BUILD`),
# so the previous explicit driver dependency is no longer needed here.
python_test_utils(name="integration-test-utils")
python_tests()
